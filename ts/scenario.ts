/* eslint-disable */

export const protobufPackage = "scenario";

/** Message Node */
export interface MessageNode {
  id: number;
  title: string;
  text: string;
}

/** HTML Node */
export interface HTMLNode {
  id: number;
  title: string;
  text: string;
}
